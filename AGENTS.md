# 仓库工作约束

本文件适用于整个仓库及其所有子目录。

## 实现原则

- 遵循 KISS、DRY、YAGNI；优先最小、直接、可维护的实现。
- 只实现当前需求，不预建推测性的扩展点、层级或通用框架。
- 复用现有概念与路径；不要创建重复的 source of truth。
- 问题应在其来源处就地修正，不用旁路、补丁层或额外流程掩盖。
- 修正后立即运行最小相关测试，再依据变更风险扩大回归范围。
- 不得为了测试便利或流程需要新增 production 抽象。
- 测试应验证可观察行为与重要不变量，而不是证明开发流程。

## 文档与治理边界

- 禁止创建开发治理型合约、证据、报告、门禁、签核、checklist、状态跟踪或平行计划文档。
- 仅当用户明确要求，或产物本身属于运行时、公开接口、法律或安全必需内容时，才可创建上述材料。
- 技术 API、数据格式、状态机 contract 及实现必需的 checksum 不属于治理文档。
- 技术 contract 只保留实现与互操作所需的最小真相，不扩展为流程证明材料。
- 发现治理 overhead 时立即删除，并把仍有价值的内容合并到既有唯一真相来源。
- 不复制已有规范、计划、注释或测试所表达的同一事实。

## 变更纪律

- 保持改动聚焦，避免顺手重构、全文件格式化和无关清理。
- 尊重现有工作区改动；未经授权不得覆盖、删除或改写。
- 新增文件前先确认现有位置无法承载该事实。
- 完成前检查实现、测试和文档是否存在重复或不必要的治理负担。
